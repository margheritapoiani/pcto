#include <stdio.h> 
int main ()
{ 
    int a;
    printf("inserisci un numero");
    scanf("%d", &a); 
    if(a%2==0)
    {
        printf(" il numero e' pari");
    
     }
     else 
     { 
        printf("il numero e'dispari");
     }
}