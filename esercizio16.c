#include <stdio.h> 
int main ()
{
    int a; 
    int b;
    int c;
    printf ("inserisci tre lunghezze"); 
    scanf("%d %d %d", &a, &b, &c);
    if ( a==b)
    {  
         if(b==c)
        {
          printf("il triangolo re'equilatero");
        }
         else if (a==b)
        {
            if (b!=c)
            { 
                printf ("il triangolo e' isoscele");
            }

        }
        else if (a!=b);
        {
            if(b!c)
            { 
                printff ("il triangolo e'scaleno");
            }
        }

    }
    
    {

    }
    if (a!=c)
    {

    }
     if()
    {
        printf (" il triangolo e'isoscele");
    }
     if (a!=b!=c)
    {  
        printf (" il triangolo e'scaleno");
    }
}